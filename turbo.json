{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "format:check": {
      "cache": true,
      "inputs": ["**/*.{ts,tsx,md}", ".prettierignore"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
